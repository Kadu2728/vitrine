"""Dominio puro: geometria, agrupamento, share e vazios.

Este pacote nao importa nada de ``vision``, ``storage``, ``batch`` ou ``render``,
e nao depende de imagem, arquivo ou modelo de ML. A seta de dependencia aponta
sempre para dentro, e ``tests/unit/test_architecture.py`` faz cumprir a regra.
"""
